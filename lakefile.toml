name = "{{PROJECT_NAME}}"
version = "0.1.0"
defaultTargets = ["{{PROJECT_NAME}}"]
lintDriver = "batteries/runLinter"

[leanOptions]
# Pretty-prints `fun a ↦ b`
pp.unicode.fun = true
# Don't assume a typo is a new variable
autoImplicit = false
relaxedAutoImplicit = false
# Enable all mathlib linters: automatically matches what mathlib uses.
weak.linter.mathlibStandardSet = true
linter.style.header = false
# Switch off warnings generated by `sorry`
warn.sorry = false

[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4.git"

# `checkdecls` provides `lake exe checkdecls`, which the docgen-action
# invokes to verify that every \lean{...} reference in the blueprint
# points to a real declaration in our Lean sources.
[[require]]
name = "checkdecls"
git = "https://github.com/PatrickMassot/checkdecls.git"
rev = "master"

# Add project-specific dependencies here. Example (pin to a specific
# revision so `lake update` runs are deliberate):
#
# [[require]]
# name = "SomeLib"
# git = "https://github.com/user/SomeLib.git"
# rev = "abc1234..."

[[lean_lib]]
name = "{{PROJECT_NAME}}"

# Add `lake exe <name>` binaries here. Example:
#
# [[lean_exe]]
# name = "my-tool"
# root = "Main"
# supportInterpreter = true
