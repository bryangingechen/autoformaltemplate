import Mathlib

/-! # {{PROJECT_NAME}}

Top-level project module. Replace this with the project's actual content
once the rename script has run and the first phase begins.
-/

namespace {{PROJECT_NAME}}

/-- Placeholder so `lake build` succeeds out of the box. Delete once
the project has real content. -/
def hello : String := "world"

end {{PROJECT_NAME}}
